# Business CRM Frontend

Профессиональный frontend для Django CRM системы в стиле Битрикс24, построенный с использованием современных технологий для создания превосходного пользовательского опыта в корпоративной среде.

## Возможности

### 🎯 Полное покрытие функционала
- **Контакты** - управление контактами клиентов
- **Компании** - управление компаниями
- **Лиды** - управление потенциальными клиентами
- **Сделки** - отслеживание продаж и сделок
- **Задачи** - управление задачами и проектами
- **Проекты** - управление проектами
- **Дашборд** - аналитика и обзор

### 🎨 Дизайн в стиле Битрикс24
- **Строгий бизнес-подход** - профессиональный корпоративный дизайн
- **Sidebar навигация** - удобная боковая панель с иконками и счетчиками
- **Карточный интерфейс** - современная подача информации
- **Цветовые индикаторы** - интуитивное понимание статусов
- **Градиентные акценты** - элегантные визуальные эффекты
- **Responsive дизайн** - адаптация под все устройства

### ⚡ Высокая производительность
- **HTMX** - быстрые интерактивные обновления
- **SPA архитектура** - без перезагрузки страниц
- **Кэширование** - быстрая загрузка данных
- **Оптимизированные запросы** - минимум обращений к API

### 📞 Интегрированная телефония
- **Софт-телефон** - встроенный диалер с клавиатурой
- **История звонков** - отслеживание всех коммуникаций
- **Интеграция с контактами** - автоматическое определение абонентов
- **DTMF тоны** - поддержка тональных сигналов
- **Управление вызовами** - hold, mute, transfer функции

## Структура проекта

```
frontend/
├── index.html              # Главная страница с Битрикс24 дизайном
├── js/
│   ├── app.js              # Основное приложение с sidebar навигацией
│   ├── ui-helpers.js       # UI компоненты и хелперы
│   ├── phone.js            # Телефонный модуль
│   ├── contacts.js         # Управление контактами
│   ├── companies.js        # Управление компаниями
│   ├── deals.js            # Управление сделками
│   ├── leads.js            # Управление лидами
│   ├── tasks.js            # Управление задачами
│   ├── projects.js         # Управление проектами
│   └── memos.js            # Управление мемо
└── README.md               # Документация
```

## Установка и настройка

### 1. Настройка Django API

Убедитесь, что Django CRM API работает и доступен по адресу `/api/`:

```python
# В settings.py Django
CORS_ALLOW_ALL_ORIGINS = True  # Для разработки
# Или настройте CORS для вашего домена

# Добавьте в INSTALLED_APPS
INSTALLED_APPS = [
    # ...
    'corsheaders',
    'rest_framework',
    'rest_framework.authtoken',
]

# Добавьте middleware
MIDDLEWARE = [
    # ...
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
]
```

### 2. Запуск frontend

#### Вариант 1: Через веб-сервер
```bash
# Используйте любой веб-сервер для статических файлов
python -m http.server 8080
# Или
npx serve .
# Или
php -S localhost:8080
```

#### Вариант 2: Интеграция в Django
Поместите файлы в директорию `static/` вашего Django проекта:

```python
# В urls.py
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    # ваши URL
] + static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
```

### 3. Настройка API Base URL

По умолчанию frontend обращается к API по адресу `{window.location.host}/api/`.

Для изменения базового URL API отредактируйте в `js/app.js`:

```javascript
constructor() {
    this.apiBase = 'http://your-django-server.com/api';  // Измените здесь
    // ...
}
```

## Использование

### Аутентификация

1. Откройте frontend приложение в браузере
2. Нажмите кнопку "Login" в правом верхнем углу
3. Введите учетные данные Django пользователя
4. После входа вы получите доступ ко всем функциям

### Навигация

- **Dashboard** - главная страница с аналитикой
- **Contacts** - список и управление контактами
- **Companies** - список и управление компаниями
- **Leads** - работа с потенциальными клиентами
- **Deals** - управление сделками и продажами
- **Tasks** - задачи и их выполнение
- **Projects** - управление проектами

### Функции

#### Контакты
- ✅ Просмотр списка контактов
- ✅ Поиск и фильтрация
- ✅ Добавление нового контакта
- ✅ Редактирование контакта
- ✅ Удаление контакта
- ✅ Просмотр детальной информации

#### Компании
- ✅ Просмотр списка компаний
- ✅ Поиск компаний
- ✅ Добавление новой компании
- ✅ Редактирование компании
- ✅ Управление статусами

#### Лиды
- ✅ Управление лидами
- ✅ Фильтрация по статусу
- ✅ Конверсия в контакты
- ✅ Отслеживание источников

#### Сделки
- ✅ Канбан-доска сделок
- ✅ Управление этапами
- ✅ Отслеживание вероятности
- ✅ Финансовая аналитика

#### Задачи
- ✅ Список задач
- ✅ Управление приоритетами
- ✅ Отслеживание сроков
- ✅ Связь с проектами

#### Проекты
- ✅ Управление проектами
- ✅ Отслеживание прогресса
- ✅ Планирование задач
- ✅ Управление этапами

## API Интеграция

Frontend использует следующие API endpoints:

```
GET    /api/contacts/        # Список контактов
POST   /api/contacts/        # Создание контакта
GET    /api/contacts/{id}/   # Детали контакта
PUT    /api/contacts/{id}/   # Обновление контакта
DELETE /api/contacts/{id}/   # Удаление контакта

GET    /api/companies/       # Список компаний
POST   /api/companies/       # Создание компании
GET    /api/companies/{id}/  # Детали компании
PUT    /api/companies/{id}/  # Обновление компании
DELETE /api/companies/{id}/  # Удаление компании

GET    /api/deals/           # Список сделок
POST   /api/deals/           # Создание сделки
GET    /api/deals/{id}/      # Детали сделки
PUT    /api/deals/{id}/      # Обновление сделки
DELETE /api/deals/{id}/      # Удаление сделки

GET    /api/leads/           # Список лидов
POST   /api/leads/           # Создание лида
GET    /api/leads/{id}/      # Детали лида
PUT    /api/leads/{id}/      # Обновление лида
DELETE /api/leads/{id}/      # Удаление лида

GET    /api/tasks/           # Список задач
POST   /api/tasks/           # Создание задачи
GET    /api/tasks/{id}/      # Детали задачи
PUT    /api/tasks/{id}/      # Обновление задачи
DELETE /api/tasks/{id}/      # Удаление задачи

GET    /api/projects/        # Список проектов
POST   /api/projects/        # Создание проекта
GET    /api/projects/{id}/   # Детали проекта
PUT    /api/projects/{id}/   # Обновление проекта
DELETE /api/projects/{id}/   # Удаление проекта

GET    /api/stages/          # Этапы сделок
GET    /api/task-stages/     # Этапы задач
GET    /api/project-stages/  # Этапы проектов
GET    /api/users/me/        # Текущий пользователь
POST   /api/auth/token/      # Аутентификация
```

## Технические детали

### Архитектура

- **Модульная структура** - каждый раздел в отдельном файле
- **Class-based подход** - использование ES6 классов
- **Event-driven** - реактивные обновления интерфейса
- **Token-based auth** - безопасная аутентификация

### Совместимость

- **Современные браузеры** - Chrome 80+, Firefox 75+, Safari 13+, Edge 80+
- **Мобильные устройства** - iOS Safari, Chrome Mobile
- **Планшеты** - полная поддержка сенсорного интерфейса

### Производительность

- **Lazy loading** - загрузка по требованию
- **Debounced search** - оптимизированный поиск
- **Minimal DOM manipulation** - быстрые обновления
- **Efficient caching** - кэширование запросов

## Кастомизация

### Изменение темы

Отредактируйте конфигурацию Tailwind в `index.html`:

```javascript
tailwind.config = {
    theme: {
        extend: {
            colors: {
                primary: {
                    50: '#eff6ff',
                    500: '#3b82f6',  // Измените основной цвет здесь
                    600: '#2563eb',
                    700: '#1d4ed8',
                }
            }
        }
    }
}
```

### Добавление новых разделов

1. Создайте новый JavaScript файл в папке `js/`
2. Добавьте класс-менеджер по образцу существующих
3. Подключите файл в `index.html`
4. Добавьте навигационную кнопку
5. Добавьте секцию в основной контент

### Локализация

Для поддержки многоязычности добавьте объект переводов:

```javascript
const translations = {
    'en': {
        'contacts': 'Contacts',
        'companies': 'Companies',
        // ...
    },
    'ru': {
        'contacts': 'Контакты', 
        'companies': 'Компании',
        // ...
    }
};
```

## Решение проблем

### CORS ошибки
```
Access to fetch at 'http://localhost:8000/api/' from origin 'http://localhost:8080' has been blocked by CORS policy
```

**Решение**: Настройте CORS в Django settings.py:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:8080",
    "http://127.0.0.1:8080",
]
```

### 401 Unauthorized
```
Error: HTTP error! status: 401
```

**Решение**: Проверьте настройки аутентификации в Django:
```python
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.TokenAuthentication',
    ],
}
```

### Медленная загрузка
**Решение**: 
- Используйте пагинацию в API
- Включите gzip сжатие в веб-сервере
- Оптимизируйте запросы с `select_related`

## Разработка

### Добавление новых функций

1. Изучите существующий код в соответствующем модуле
2. Следуйте установленным паттернам
3. Добавьте обработку ошибок
4. Обновите интерфейс

### Отладка

Используйте инструменты разработчика браузера:
- **Console** - для просмотра ошибок JavaScript
- **Network** - для мониторинга API запросов
- **Elements** - для отладки CSS/HTML

## Лицензия

Этот frontend является частью Django CRM проекта и наследует его лицензию.

## Поддержка

Для получения поддержки:
1. Проверьте этот README
2. Изучите код в соответствующих модулях
3. Создайте issue в репозитории проекта